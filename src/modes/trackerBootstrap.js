// Planned internal dashboard bootstrap (admin/consultant views).
export async function bootstrapTrackerMode() {
  throw new Error('bootstrapTrackerMode() is a rewrite skeleton and is not wired yet.');
}
