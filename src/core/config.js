// Planned home for environment flags and runtime configuration.
export const DEFAULT_REASSIGN_CUTOFF = '2026-02-17';

export function isLocalhost(hostname) {
  return hostname === 'localhost' || hostname === '127.0.0.1';
}
