// Planned API client for /api/data bootstrap loading and admin auth flow.
export async function fetchBootstrapData() {
  throw new Error('fetchBootstrapData() is a rewrite skeleton and is not wired yet.');
}
