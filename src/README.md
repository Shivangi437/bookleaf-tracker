# Modular Rewrite Skeleton (Not Yet Wired)

This folder is a planned extraction target for the current `public/app.js`.

- These files are scaffolds only.
- No runtime behavior is changed by adding this directory.
- The live app still runs from `public/app.js`.
